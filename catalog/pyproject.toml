[project]
name = "catalog"
version = "0.1.0"
description = "Work with STAC catalog and items to create bounding boxes"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas",
    "geopandas",
    "shapely",
    "obstore",
    "pyarrow",
]

[project.optional-dependencies]
dev = [
    "duckdb",
]
