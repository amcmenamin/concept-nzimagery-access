[project]
name = "imagery-read-rasterio"
version = "0.1.0"
description = "Read imagery from S3 using rasterio approach."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.42.94",
    "rasterio>=1.5.0",
]
